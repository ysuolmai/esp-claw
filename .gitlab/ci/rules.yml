############
# Patterns #
############

# build system, if changed, build all apps
.patterns-build_system: &patterns-build_system
  # For test
  # - "tools/build_apps.py"
  - "conftest.py"
  - "pytest.ini"

# components folder, in the alphabetic order
.patterns-components_capabilities: &patterns-components_capabilities
  - "components/claw_capabilities/**/*"

.patterns-components_modules: &patterns-components_modules
  - "components/claw_modules/**/*"

.patterns-components_lua_modules: &patterns-components_lua_modules
  - "components/lua_modules/**/*"

.patterns-components_common: &patterns-components_common
- "components/common/**/*"

# application folder, in the alphabetic order
.patterns-application_mcp_server_point: &patterns-application_mcp_server_point
  - "application/mcp_server_point/**/*"

.patterns-application_edge_agent: &patterns-application_edge_agent
  - "application/edge_agent/**/*"

.patterns-docs: &patterns-docs
  - "docs/**/*"

##############
# if anchors #
##############

# Push to any protected ref
.if-protected-push: &if-protected-push
  if: '$CI_COMMIT_REF_PROTECTED == "true" && $CI_PIPELINE_SOURCE == "push"'

# master push only
.if-master-push: &if-master-push
  if: '$CI_COMMIT_REF_NAME == "master" && $CI_PIPELINE_SOURCE == "push"'

# Merge request pipeline (dev branches). Non-protected branch push pipelines are
# disabled at workflow level (see .gitlab-ci.yml), so we only care about MR here.
.if-mr: &if-mr
  if: '$CI_PIPELINE_SOURCE == "merge_request_event"'

##################
# Auto Generated #
##################
.if-trigger-job: &if-trigger-job
  if: "$BOT_DYNAMIC_TEST_JOBS && $BOT_DYNAMIC_TEST_JOBS =~ $CI_JOB_NAME"

.if-label-build: &if-label-build
  if: '$BOT_LABEL_BUILD || $CI_MERGE_REQUEST_LABELS =~ /^(?:[^,\n\r]+,)*build(?:,[^,\n\r]+)*$/i'

.if-label-pre_check: &if-label-pre_check
  if: '$BOT_LABEL_PRE_CHECK || $CI_MERGE_REQUEST_LABELS =~ /^(?:[^,\n\r]+,)*pre_check(?:,[^,\n\r]+)*$/i'

#########
# Rules #
#########

.rules:build:application_edge_agent:
  rules:
    - <<: *if-protected-push
    - <<: *if-label-build
    - <<: *if-trigger-job
    - <<: *if-mr
      changes: *patterns-build_system
    - <<: *if-mr
      changes: *patterns-components_capabilities
    - <<: *if-mr
      changes: *patterns-components_modules
    - <<: *if-mr
      changes: *patterns-components_lua_modules
    - <<: *if-mr
      changes: *patterns-components_common
    - <<: *if-mr
      changes: *patterns-application_edge_agent
    - <<: *if-mr
      changes: *patterns-docs
    - when: never

.rules:build:application_mcp_server_point:
  rules:
    - <<: *if-protected-push
    - <<: *if-label-build
    - <<: *if-trigger-job
    - <<: *if-mr
      changes: *patterns-build_system
    - <<: *if-mr
      changes: *patterns-components_capabilities
    - <<: *if-mr
      changes: *patterns-components_modules
    - <<: *if-mr
      changes: *patterns-components_lua_modules
    - <<: *if-mr
      changes: *patterns-components_common
    - <<: *if-mr
      changes: *patterns-application_mcp_server_point
    - <<: *if-mr
      changes: *patterns-docs
    - when: never

.rules:pre_check:docs:
  rules:
    - <<: *if-protected-push
    - <<: *if-label-pre_check
    - <<: *if-trigger-job
    - <<: *if-mr
      changes: *patterns-docs
    - when: never

.rules:build:doc:
  rules:
    - <<: *if-protected-push
    - <<: *if-label-build
    - <<: *if-trigger-job
    - <<: *if-mr
      changes: *patterns-docs
    - when: never
